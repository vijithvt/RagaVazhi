import { CorrectionForm } from "@/components/correction-form";
export default async function CorrectionsPage({ searchParams }: { searchParams: Promise<{ entity?: string }> }) { const { entity } = await searchParams; return <><section className="page-hero"><div className="container"><span className="eyebrow">Community corrections</span><h1 className="serif">Help the catalog become more accurate</h1><p className="muted">Include a source when possible. Editors review every report before changing a published record.</p></div></section><section className="section"><div className="container" style={{ maxWidth: 720 }}><CorrectionForm entity={entity} /></div></section></>; }
